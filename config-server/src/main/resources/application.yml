
server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:
          search-paths:
          username:
          password:
      label: master
